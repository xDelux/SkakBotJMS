/*@Ignore
    @Test
    public void expectedPositionsAtDepth() {
        algo.runAlphaBeta();
    }

    @Test
    public void alphaBetaRunTimeAtDepth() {
        StopWatch stopWatch = new StopWatch();
        for (int i = 1; i <= 5; i++) {
            algo.setDepth(i);
            stopWatch.start();
            algo.runAlphaBeta();
            stopWatch.stop();
            System.out.println("TIME: " + stopWatch.getTime(TimeUnit.SECONDS) + " | DEPTH: " + i);
            stopWatch.reset();
        }
    }*